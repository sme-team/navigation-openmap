{
  "name": "dqcai-blog-be",
  "version": "1.3.7",
  "type": "module",
  "description": "Blog API built with Fastify, TypeScript and MongoDB using @dqcai/mongodb library",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && npm run build:assets",
    "start": "node dist/server.js",
    "build:assets": "node -e \"const fs=require('fs'); const path=require('path'); try{fs.cpSync('public', 'dist', {recursive:true, force:true}); console.log('✅ Assets copied');}catch(e){console.log('ℹ️ No assets to copy or already exists');}\"",
    "clean": "npm run clean:cross-platform",
    "clean:cross-platform": "node -e \"const fs=require('fs'); const path=require('path'); function rmrf(dir){if(fs.existsSync(dir)){fs.rmSync(dir,{recursive:true,force:true});}} rmrf('./dist'); rmrf('./build'); console.log('✅ Clean completed');\"",
    "dev": "tsx watch server.ts",
    "dev:init": "tsx scripts/init-db.ts && tsx watch server.ts",
    "test": "tsx test/blog-server.test.ts",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    "db:init": "tsx scripts/init-db.ts",
    "db:seed": "tsx scripts/seed-data.ts",
    "db:cleanup": "tsx scripts/cleanup-db.ts",
    "db:status": "tsx scripts/db-status.ts",
    "pre-release:check": "npm run lint && npm run type-check && npm run test && npm run build && echo '✅ All pre-release checks passed'",
    "release:patch": "npm run release:execute -- patch",
    "release:minor": "npm run release:execute -- minor",
    "release:major": "npm run release:execute -- major",
    "release:execute": "node scripts/release.js",
    "release:manual": "echo '🚀 Starting manual release process...' && git status --porcelain && echo 'Current branch:' && git branch --show-current && echo 'Ready for manual release. Follow the steps in README.md'",
    "postversion": "echo '📦 Version updated to:' && npm pkg get version",
    "release:status": "echo 'Current branch:' && git branch --show-current && echo 'Version:' && npm pkg get version && echo 'Git status:' && git status --short"
  },
  "keywords": [
    "blog",
    "api",
    "fastify",
    "typescript",
    "mongodb",
    "cms"
  ],
  "author": "Đoàn Quốc Cường",
  "license": "MIT",
  "dependencies": {
    "@dqcai/mongodb": "^1.1.0",
    "@fastify/cors": "^11.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@fastify/type-provider-typebox": "^5.2.0",
    "@sinclair/typebox": "^0.34.41",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.0",
    "google-auth-library": "^10.3.0",
    "googleapis": "^160.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.19.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.5.1",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^9.35.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "optionalDependencies": {
    "@esbuild/win32-x64": "^0.25.10"
  }
}
